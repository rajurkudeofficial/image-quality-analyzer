# ==========================================================
# Digital Image Quality Analyzer — Backend Requirements
# ==========================================================
# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# Image processing / analysis
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# AI Enhancement (Real-ESRGAN stack)
# Intentionally disabled for the low-memory cloud deployment.
# Install a compatible PyTorch/Real-ESRGAN stack separately if GPU
# infrastructure is available later.
# torch==2.3.1
# torchvision==0.18.1
# basicsr==1.4.2
# realesrgan==0.3.0

# Utilities
aiofiles==23.2.1
python-dotenv==1.0.1
httpx==0.27.0
